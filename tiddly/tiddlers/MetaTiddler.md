# 配置文件
- 在 `tiddlywiki.info` 里面配置 `    "plugins": [ "tiddlywiki/markdown"],` 即可获得对于markdown的支持

# 尝试使用TiddlyWiki
- 在Tiddlywiki里面如果需要检索其他Tiddler的话，直接使用 `CamelCase` 的其他Tiddler的标题即可。
- 如若不想这个标题以一个Tiddler链接的形式在Tiddler内出现，则需要在前面加上`~`来避免自动识别
